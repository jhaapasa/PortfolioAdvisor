# Core paths are passed via CLI: --input-dir, --output-dir

# Gemini / LLM (optional for stubbed runs)
GEMINI_API_KEY=
GEMINI_API_BASE=
GEMINI_MODEL=gemini-1.5-pro
REQUEST_TIMEOUT_S=60
MAX_TOKENS=
TEMPERATURE=0.2

# Logging
LOG_LEVEL=INFO
LOG_FORMAT=plain

# Parser (LLM Parsing Agent)
PARSER_MAX_CONCURRENCY=4
PARSER_MAX_RPM=60
PARSER_MAX_RETRIES=2
PARSER_MAX_DOC_CHARS=20000


